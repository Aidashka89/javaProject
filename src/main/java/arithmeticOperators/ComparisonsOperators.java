package arithmeticOperators;

public class ComparisonsOperators {
    public static void main(String[] args) {
        /*
        Comparisons operators are used to compare 2 values:
        > more than
        <less than
        >= more or equal
        <= less or equal
        == equal
        != not equal
         */
         byte potato = 5;
         byte fries = 10;
          System.out.println(potato> fries); // false

    }
}
